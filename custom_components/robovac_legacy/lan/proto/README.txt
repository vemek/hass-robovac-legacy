Local LAN wire format shared with archived PyRobovac / lakeside-derived clients.

Rebuild generated Python stubs (from repo root):
  bash scripts/regen_proto.sh

Version pins live in scripts/requirements_proto.txt (match Home Assistant
Core package_constraints.txt for protobuf/grpcio-tools when practical).
